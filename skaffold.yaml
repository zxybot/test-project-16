apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: changeme
    context: .
    docker: {}
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/zxybot/test-project-16:{{.VERSION}}'
  local: {}
deploy:
  kubectl: {}
profiles:
- name: dev
  build:
    artifacts:
    - docker: {}
    tagPolicy:
      envTemplate:
        template: '{{.DOCKER_REGISTRY}}/zxybot/test-project-16:{{.DIGEST_HEX}}'
    local: {}
  deploy:
    helm:
      releases:
      - name: test-project-16
        chartPath: charts/test-project-16
        setValueTemplates:
          image.repository: '{{.DOCKER_REGISTRY}}/zxybot/test-project-16'
          image.tag: '{{.DIGEST_HEX}}'
